// import FreshLoad from "@/components/home/FreshLoad";
// import { cookies } from "next/headers";
export default function page() {
  //   const cookieStore = cookies();
  //   const cookieToken = cookieStore.get("next-auth.session-token");
  // console.log("check cookie", cookieToken);

  // let limit = 12;
  // let genre: string | null;
  // const res = await fetch(
  //   `${process.env.HTTP_API_URL}/api/animes?limit=${limit}&&${(genre = "")}`,

  //   {
  //     method: "GET",
  //     next: { tags: ["get-animes-genre"] },
  //   }
  // );
  // const data = await res.json();
  // console.log("check data", data);
  return <main className=" min-h-screen text-neutral-900 ">404 not found</main>;
}
